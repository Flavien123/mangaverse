# Manga Reader App

Современное веб-приложение для чтения манги с использованием API MangaDex, личной библиотекой и синхронизацией прогресса.

## ✨ Основные функции

*   **Каталог манги**: Просмотр новинок, поиск, фильтрация по жанрам и статусу (через MangaDex API).
*   **Чтение**: Удобный ридер с вертикальной прокруткой, поддержкой горячих клавиш и сохранением прогресса.
*   **Личный кабинет**:
    *   **Закладки**: Сохранение любимой манги.
    *   **Статусы**: Отслеживание (В планах, Читаю, Прочитано, Брошено, Отложено).
    *   **История**: Автоматическое сохранение последней прочитанной главы и страницы.
    *   **Оценки**: Возможность ставить оценку манге (1-10).
*   **Адаптивность**: Полная поддержка мобильных устройств и десктопов.
*   **PWA**: Возможность установки как приложения (в планах).

## 🛠 Технологии

### Frontend
*   **React** (Vite) — основной фреймворк.
*   **Tailwind CSS** — стилизация и адаптивность.
*   **Bun** — пакетный менеджер и сборщик.

### Backend
*   **FastAPI** (Python) — высокопроизводительный REST API.
*   **SQLAlchemy (Async)** — ORM для работы с базой данных.
*   **PostgreSQL** — основная база данных.
*   **Pydantic** — валидация данных.
*   **PyJWT** — JWT аутентификация.
*   **uv** — современный менеджер пакетов Python.

### DevOps
*   **Docker & Docker Compose** — контейнеризация и оркестрация.
*   **Nginx** — веб-сервер для раздачи статики и проксирования API.

## 📂 Структура проекта

```
manga/
├── backend/                # Серверная часть (FastAPI)
│   ├── src/
│   │   ├── routers/        # API эндпоинты (auth, user)
│   │   ├── auth.py         # Логика аутентификации
│   │   ├── database.py     # Подключение к БД
│   │   ├── models.py       # SQLAlchemy модели
│   │   ├── schemas.py      # Pydantic схемы
│   │   └── main.py         # Точка входа
│   ├── Dockerfile
│   └── pyproject.toml
│
├── frontend/               # Клиентская часть (React)
│   ├── src/
│   │   ├── api/            # Клиенты API (MangaDex, Backend)
│   │   ├── components/     # Переиспользуемые компоненты
│   │   ├── context/        # React Context (Auth)
│   │   ├── pages/          # Страницы приложения
│   │   └── styles/         # Глобальные стили
│   ├── Dockerfile
│   └── nginx.conf
│
├── docker-compose.yml      # Конфигурация Docker сервисов
├── deploy.sh               # Скрипт для быстрого деплоя
└── README.md
```

## 🚀 Инструкция по запуску

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <repository_url>
    cd manga
    ```

2.  **Запустите скрипт деплоя:**
    ```bash
    chmod +x deploy.sh
    ./deploy.sh
    ```
    *Или вручную:* `docker compose up --build -d`

    *   Frontend: [http://localhost](http://localhost)
    *   Backend API: [http://localhost:8000](http://localhost:8000)
